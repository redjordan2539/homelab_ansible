# =======================================================
# .zshrc
# Auto-Generated by Ansible
# Do not modify this file directly
# Generated for {{ ansible_user }} on {{ ansible_host }}
# =======================================================

safe_source() {
    if [ -f "$HOME/dotfiles/zsh/configs/$1.zsh" ]; then
    source "$HOME/dotfiles/zsh/configs/$1.zsh"
fi
}

# Global zsh includes
{% for file in all_zsh_includes %}
safe_source "{{ file }}"
{% endfor %}

{% if host_zsh_includes %}
# Host zsh includes
{% for file in host_zsh_includes %}
safe_source "{{ file }}"
{% endfor %}
{% endif %}

unset -f safe_source